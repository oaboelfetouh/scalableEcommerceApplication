const express = require("express");
const { AuthMiddleware } = require("../middlewares/authentication.middlewares");
const router = express.Router();


// here we should implement the routes to our endpoints


module.exports={router};